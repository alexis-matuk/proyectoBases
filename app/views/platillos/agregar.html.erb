
<%= flash[:error] %>

<h1>Listing Platillos <%= link_to 'New Platillo', new_platillo_path, class: "btn btn-success pull-right" %></h1>



<div>

<div class="row">
   <div class="col-lg-12">


<table class="table table-striped table-advance table-hover">
  <tbody>
  <tr>
    <th><i class="icon_map"></i>Nombre</th>
    <th><i class="icon_phone"></i> Descripción</th>
    <th><i class="icon_currency"></i> Precio</th>
  </tr>

    <% @platillos.each do |platillo| %>
      <tr>
        <td><%= link_to platillo.nombre, llenar_establecimiento_platillo_path(ide: @establecimiento.id, id: platillo.id) %></td>
        <td><%= platillo.descripcion %></td>
        <td><%= platillo.precio%></td>

      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>

<br>





<br>