<%= form_for (@platillo) do |f| %>
  <% if @platillo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@platillo.errors.count, "error") %> prohibited this platillo from being saved:</h2>

      <ul>
      <% @platillo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :establecimiento_id, :value => @establecimiento.id %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="field">
    <%= f.label :precio %><br>
    <%= f.text_field :precio %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
