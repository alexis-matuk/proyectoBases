<h1>Listing ingrediente_platillos</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ingrediente_platillos.each do |ingrediente_platillo| %>
      <tr>
        <td><%= link_to 'Show', ingrediente_platillo %></td>
        <td><%= link_to 'Edit', edit_ingrediente_platillo_path(ingrediente_platillo) %></td>
        <td><%= link_to 'Destroy', ingrediente_platillo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ingrediente platillo', new_ingrediente_platillo_path %>
