<h1>Listing establecimiento_proveedors</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @establecimiento_proveedors.each do |establecimiento_proveedor| %>
      <tr>
        <td><%= link_to 'Show', establecimiento_proveedor %></td>
        <td><%= link_to 'Edit', edit_establecimiento_proveedor_path(establecimiento_proveedor) %></td>
        <td><%= link_to 'Destroy', establecimiento_proveedor, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Establecimiento proveedor', new_establecimiento_proveedor_path %>
