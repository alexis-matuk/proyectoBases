<%= form_for(@ingrediente) do |f| %>
  <% if @ingrediente.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(@ingrediente.errors.count, "error") %> prohibited this ingrediente from being saved:</h2>

      <ul>
      <% @ingrediente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :proveedor_id %>



<div class="row">
    <div class="col-lg-12">
        <section class="panel">
            <header class="panel-heading">
                Datos
            </header>
            <div class="panel-body">
                <div class="form">
                    <form class="form-validate form-horizontal" id="feedback_form" method="get" action="">
                        <div class="form-group ">
                        <div class="row">
                            <label for="cname" class="control-label col-lg-2">Nombre <span class="required">*</span></label>
                          </div>
                          <div class="row">
                            <div class="col-lg-12">
                         
                                <%= f.text_field :nombre,{:class => "form-control"} %>

                            </div>
                            </div>
                        </div>
                        <div class="form-group ">
                        <div class="row">
                            <label for="cemail" class="control-label col-lg-2">Precio <span class="required">*</span></label>
                        </div>
                        <div class="row">
                            <div class="col-lg-12">
                            
                                <%= f.text_field :precio , {:class => "form-control"} %>
                            </div>
                          </div>
                        </div>                                     
                        <div class="form-group">
                            <div class="col-lg-12">
                            <br>
                                <div class="actions">
                                 <%= link_to 'Back',ingredientes_path,{:class => "btn btn-warning"} %>
                                  <%= f.submit "Aceptar" ,{:class => "btn btn-primary col-lg-6 pull-right"}%>
                                </div>

                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </section>
    </div>
</div>


<% end %>
